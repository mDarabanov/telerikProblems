// let phones = ['Nokia', 'Samsung', 'Huawei', 'Iphone'];

// phones.forEach(function(models) {
//   models += ' Mobile Phone';
//   models = models.toUpperCase();
//   // return models;
//   console.log(models);
// });

words = ['cat', 'in', 'hat'];
words.forEach(function(word, num, all) {
  console.log('Word ' + num + ' in ' + all.toString() + ' is ' + word);
});
